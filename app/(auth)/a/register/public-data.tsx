import Input from 'components/molecules/Input/Input';
import { as } from 'constants/styles';
import { View } from 'react-native';

export default () => {
  return (
    <View style={as.container}>
      <Input />
    </View>
  );
};
